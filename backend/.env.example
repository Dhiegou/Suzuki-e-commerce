# ── Server ──
PORT=3001
NODE_ENV=development

# ── SQL Server ──
DB_DIALECT=sqlserver
DB_HOST=oracle.fiap.com.br
DB_PORT=1521
DB_USER=rm560294
DB_PASSWORD=170904
DB_NAME=Suzuki

# ── CORS ──
CORS_ORIGIN=http://localhost:3000

# ── Security ──
# Used for hashing sensitive data (CPF)
HASH_SALT=your_random_salt_here_change_in_production
